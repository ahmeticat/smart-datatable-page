{
  "name": "smart-datatable-page",
  "version": "0.2.7",
  "description": "Simple table extension with sorting, filtering, exporting ... for Angular apps",
  "repository": "ahmeticat/smart-datatable",
  "license": "MIT",
  "bugs": "https://github.com/ahmeticat/smart-datatable/issues",
  "devDependencies": {
    "stylelint": "^10.0.1",
    "stylelint-config-primer": "^8.0.0",
    "stylelint-selector-no-utility": "^4.0.0"
  },
  "dependencies": {
    "@primer/css": "^12.7.0"
  },
  "scripts": {
    "test": "stylelint '**/*.scss'"
  }
}
